/**
 * Enum for different input field types
 */
export enum FieldType {
  Text = 'text',
  Email = 'email',
  Phone = 'phone',
  Password = 'password',
  Number = 'number',
  Date = 'date',
  Select = 'select',
  Autocomplete = 'autocomplete',
  Textarea = 'textarea',
  Image = 'image',
  Address = 'address',
}
